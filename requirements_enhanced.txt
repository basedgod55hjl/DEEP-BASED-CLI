# Enhanced BASED GOD CLI Requirements
# DeepSeek API integration - minimal dependencies

# Core dependencies
typing-extensions>=4.0.0

# LangChain ecosystem (minimal for DeepSeek)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-core>=0.1.0
langchain-community>=0.0.10
langchain-chroma>=0.1.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pandas>=1.5.0
openai>=1.0.0
requests>=2.31.0
aiohttp>=3.8.0
beautifulsoup4>=4.12.0
rich>=13.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# LLM providers (DeepSeek via OpenAI SDK)

# Web scraping

# Data analysis

# File processing

# Rich terminal output (optional but recommended)

# Testing

# Additional utilities
# Note: Built-in modules (uuid, hashlib, datetime, json, csv, xml, re, collections, etc.) 
# are included with Python and don't need to be installed

# Database and Vector Storage
aiosqlite>=0.19.0
qdrant-client[fastembed]>=1.7.0
fastembed>=0.1.0