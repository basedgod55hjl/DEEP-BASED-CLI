{
  "name": "deep-cli-ts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "build:prod": "tsc -p . && npm run analyze",
    "start": "node dist/cli/index.js",
    "test": "jest --passWithNoTests",
    "analyze": "node scripts/analyze-bundle.js",
    "dev": "tsc -p . --watch",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "openai": "^4.8.0",
    "axios": "^1.6.5",
    "chalk": "^5.3.0",
    "commander": "^11.0.0",
    "pino": "^8.17.0",
    "pino-pretty": "^9.2.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^8.6.0",
    "@xenova/transformers": "^2.13.0",
    "onnxruntime-node": "^1.18.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/node": "^20.5.1",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}